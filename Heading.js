function drawHeading() {
    fill(color('#1A324D'));
    noStroke();
    rect(0, 0, width, 65);
    textAlign(LEFT, TOP);
    textSize(30);
    textStyle(BOLD);
    fill(white);
    text('SJC 150 Dinner - Seating Plan System', 10, 15);
}
